/* Written by tools/import_herbivores.py. Empty until the verified global
 * Herbivores dataset is imported; the maps then colour sample pixels on one
 * shared log10(Herbivores + 1) scale. Never hand-edit values here. */
window.NKUHomeAbundance = { status: 'pending', points: [] };
