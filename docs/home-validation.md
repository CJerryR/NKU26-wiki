# 首页验证记录

日期：2026-09-22。实施范围为当前目录 `nku-wiki/` 副本；从原仓库 commit `a0e3f89ff41b67757397ec8043b8e2a5d3b840ca` 的静态站源码复制。原站在开始前构建及审计通过、浏览器无警告/错误，具体基线见 `home-prototype-contract.md`。

## 构建与完整性

- `python3 build.py`：成功生成 31 页，14 hidden、0 draft。hidden 是既有搜索显示设置，不表示页面不存在。
- `python3 tools/audit_wiki_content.py --generated --generated-root public`：PASS。审计现包含首页 partial 的实际内容，继续检查模板残留、错误链接、资源、锚点和证据用语。
- 所有新增 JS 通过 Node 语法检查。
- 首页只有一个 h1，没有重复 id、空 href 或 `href="#"`，图片都有 alt。
- 11 个区段锚点齐全，覆盖六个叙事主题。
- 30 份非首页 `_content/*.html` 与开始前 SHA-256 一致。原全局 CSS、JS、内页正文保持不变；首页 CSS/JS 仅在首页加载。
- `/nku26/` 子路径下，37 个首页本地链接和资源逐个 HTTP 请求均为 200；沿用原构建器的相对前缀。

## 浏览器和交互

- Codex 实际浏览器检查了桌面首屏、土壤探索、世界和中国地图、四幅侵染分镜、化学线索、组合假设、传感器、协作关系和手机布局。
- 桌面 1440px、1080px，手机 390px、320px：文档没有横向溢出；390/320px 的全部可见正文标题、段落、按钮和链接没有越出视口。
- 主预览实际完成三条线索，状态变为 discovered，并显示 Signal detected! 和暖黄色光圈。
- 直接跳过进入真实 `#global-story`，状态为 skipped。发现后继续保留 discovered。终态幂等。
- 普通下滚先局部照亮世界标题，再扩大光圈显示地图；跳过、锚点访问和键盘焦点不被遮罩阻碍。不锁定页面滚动。
- 点击 China 进入 `#china-story`，三张中国图均有独立指标说明和明确未接数据状态。
- 传感器按钮可以依次点亮拟议链路。
- 实际浏览器从 `/nku26/` 的 Explore 入口进入 `/nku26/engineering/`，前缀为 `../`；详细页没有加载任何 home 样式，保持原布局。
- 手机导航展开正常；搜索 ascaroside 返回 9 条结果；Escape 关闭搜索并解除搜索滚动限制。
- 浏览器未见新增控制台警告/错误或破图。
- 最终整合版在独立 Chrome 的390×844减少动画模式下：11区段可见、遮罩为0、活动动画为0、body滚动保持可用；菜单和Explore能进入详细页，三次触摸引导仍能完成发现，无pageerror/console error/请求失败。
- 最终整合版390×844关闭JavaScript：11区段可读，首页宽度390px；导航7项（含logo）和Explore8项共15次真实点击均进入预期页面，HTTP200、正文和h1存在。
- A 执行者在独立真实 Chrome 完成鼠标、键盘 Enter/Space、390px 真实触摸及滚动、跳过、一次性事件、减少动画和无 JavaScript 检查。减少动画取消闪烁，仍可发现与阅读。
- C 执行者检查无脚本版本，全部科学区段、四步链路和文字可读；脚本专用按钮隐藏，实际 Results 锚点可达。
- B执行者在390px验证来源details展开、单位与无记录状态可读，四图加载，宽度无溢出；无脚本地图details和锚点可用。
- B 执行者完成 36 项数据验证及渲染交互检查：真实数据接口与占位数据分开；null/非法投影等异常不会伪造数据；清空记录后清除旧标记。

## 真实待补项

1. 地图：D01 科学分布图与 D02 0913 HP 支线的版本差异仍需核定。当前按 D01 提供可替换科学图位；需正式数据、字段、单位、年份、图例分级及来源，未编造丰度、分布或损失。
2. 专业素材：侵染、传感器和协作图为可替换原型示意，等待团队协作的正式矢量稿。无需为了继续评审补齐所有成品。
3. 成果：当前引用已有 Results 的 0528 阴性观察和 0607 作者初步 PCR 结论，并显示未完成的传感性能证据位；正式首页 3–5 项成果与原图需团队审核。没有宣称传感器性能已验证。
4. 赞助与伙伴：等待已确认名称、具体支持与允许使用的徽标；当前不展示未经证明的支持。

ascr#3/#18 组合是项目假设。甜菜黄素链路按较新 D01 表述为拟议设计；旧详细页保持原内容。风险预检不等于确诊，专业确认仍在使用路径中。

源目录在副本迁移完成时已恢复 Git 干净。随后其他并行工作在原目录新增 Astro 配置等文件；本任务未处理或回滚这些变动，本次交付仅涉及此副本。

## 已存在的内页问题

Model 内页在390×844、关闭JavaScript的手机环境中，文档宽度为599px，溢出209px。已只读启动原站 public 做相同条件复验，原站与副本结果一致；旧站仅加载原有 css/style.css。此问题不由首页改动引入，本轮按要求保留其他页面，未扩展修改内页。

最终窄屏复核还修正了320px下 Explore 段吉祥物与文案的重叠；修复后矩形不再相交，文档宽度仍为320px。
